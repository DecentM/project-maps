id: backend
language: typescript
platform: node
type: application

tasks:
  dev:
    command: tsx
    args:
      - --watch
      - src/main.ts
    options:
      cache: false
      envFile: .env

  build:
    deps:
      - proto:build
    command: tsc
    inputs:
      - src
      - tsconfig.json
      - package.json
    outputs:
      - dist
