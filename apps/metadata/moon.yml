id: metadata
language: typescript
platform: node
type: application

tasks:
  up:
    command: tsx
    args:
      - --watch
      - src/index.ts
    options:
      cache: false
      envFile: .env
      persistent: true
