id: overpass
language: typescript
platform: node
type: application

tasks:
  dev:
    command: tsx
    args:
      - --watch
      - src/index.ts
    options:
      persistent: true
      cache: false
      envFile: .env

  up:
    deps:
      - dev
    options:
      persistent: true
