project:
  name: logging
  description: 'Logging package'

tasks:
  build:
    inputs:
      - src
    outputs:
      - dist
    command:
      - tsc
    options:
      cache: true
      persistent: false
