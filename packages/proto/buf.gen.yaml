version: v2

managed:
  enabled: true

plugins:
  - local: protoc-gen-es
    out: dist/web

  - local: protoc-gen-ts
    out: dist/node
    opt:
      - no_namespace
      - unary_rpc_promise=true
      - target=ts

inputs:
  - directory: src
