id: map-style
language: typescript
platform: node
type: library

tasks:
  build:
    command:
      - tsc
    inputs:
      - src
    outputs:
      - dist
    options:
      cache: true
      persistent: false
